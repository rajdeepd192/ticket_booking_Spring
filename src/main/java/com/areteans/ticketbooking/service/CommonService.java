package com.areteans.ticketbooking.service;

import com.areteans.ticketbooking.models.Ticket;
import lombok.RequiredArgsConstructor;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.stereotype.Service;

import java.util.Map;

//@Service
//@RequiredArgsConstructor
//public class CommonService {
   // private final JdbcTemplate jdbcTemplateForCreate;

    //public Ticket create(Ticket ticket) {
       // Map<String, Object> map1 = jdbcTemplateForCreate.queryForMap("insert into ticket(ticket_no,price,start_city,end_city,train_no) values(?,?,?,?,?) RETURNING ticket_no",
                //ticket.getTicket_no(),
               // ticket.getPrice(),
               // ticket.getStart_city(),
               // ticket.getEnd_city(),
                //ticket.getTrain_no());

        // Map<String,Object> map2 = jdbcTemplateForCreate.queryForMap("insert into passengers(passenger_name,age,contact_no,start_city,end_city,ticket_no) values(?,?,?,?,?,?)",

        //}
   // }
//}
